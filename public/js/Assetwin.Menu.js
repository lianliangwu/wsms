var Assetwin.Menu = function ( editor ) {

	var container = new UI.Panel();

	var head = new UI.Panel();
	container.add(head);

	var footer = new UI.Panel();
	container.add(footer);


	return container;

}
